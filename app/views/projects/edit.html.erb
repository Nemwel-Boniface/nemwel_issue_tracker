<% provide(:title, 'Edit Project') %>

<div class="container mt-5">
  <div class="col-md-8 offset-md-2">
    <h2 class="mb-4">Edit Project</h2>
    <%= render 'form', project: @project %>
    <%= link_to 'Back', project_path(@project), class: "btn btn-link mt-3" %>
  </div>
</div>